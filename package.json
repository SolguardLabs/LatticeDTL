{
  "name": "lattice-dtl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test \"tests/node/*.test.js\"",
    "test:all": "npm run build && npm test",
    "ci": "node scripts/build.mjs && node --test \"tests/node/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  }
}
